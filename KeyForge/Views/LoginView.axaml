<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:KeyForge.ViewModels"
             x:Class="KeyForge.Views.LoginView"
             x:DataType="vm:LoginViewModel">
    
    <StackPanel>
        <Button Content="Neuer Nutzer"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Margin="5"
                Command="{Binding NavCreateUserCommand}"/>
        <StackPanel VerticalAlignment="Center" Margin="0,200,0,0" Width="300" HorizontalAlignment="Center" >
            <Border>
                <StackPanel>
                    <TextBlock Text="Anmelden"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               FontSize="24"
                               Margin="10"/>

                    <Label Content="Nutzername"
                           Target="UsernameBox"
                           HorizontalAlignment="Left"/>

                    <TextBox Watermark="Bitte gib deinen Nutzernamen ein..."
                             Text="{Binding Username}"
                             Width="300"
                             x:Name="UsernameBox"
                             HorizontalAlignment="Left"
                             Margin="0,10,0,0"/>

                    <Label Content="Masterpasswort"
                           Target="PasswordBox"
                           HorizontalAlignment="Left"
                           Margin="0,10,0,0"/>

                    <TextBox x:Name="PasswordBox"
                             Watermark="Bitte gib dein Masterpasswort ein..."
                             Text="{Binding MasterPassword}"
                             Width="300"
                             PasswordChar="*"
                             HorizontalAlignment="Left"
                             Margin="0,10,0,0">
                        <TextBox.InnerRightContent>
                            <ToggleButton 
                                Content="👁" 
                                IsChecked="{Binding #PasswordBox.RevealPassword}"
                                VerticalAlignment="Center"/>
                        </TextBox.InnerRightContent>
                        
                    </TextBox>
                    
                    <Button Content="Einloggen"
                            HorizontalAlignment="Center"
                            Margin="10"
                            HotKey="Return"
                            Command="{Binding LoginCommand}" />
                    
                </StackPanel>
            </Border>
        </StackPanel>
    </StackPanel>
</UserControl>
